<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Back Icon positioned on the left side -->
    <ImageButton
        android:id="@+id/backButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_arrow_back_24"
        android:contentDescription="Back"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:layout_alignParentStart="true"
        android:layout_marginTop="16dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center"
        android:layout_below="@id/backButton"
        android:layout_centerHorizontal="true">

        <!-- Profile Picture -->
        <ImageView
            android:id="@+id/ivProfilePicture"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:background="@drawable/rounded_edittext_background"
            android:contentDescription="Profile Picture"
            android:padding="8dp"
            android:scaleType="centerCrop"
            android:clipToOutline="true" />


        <!-- Heading Text -->
        <TextView
            android:id="@+id/headingProfileTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Don't wear sunglasses, look straight ahead and make sure you're alone"
            android:textStyle="bold"
            android:textSize="28sp"
            android:padding="8dp"
            android:paddingBottom="25dp"
            android:paddingTop="20dp" />
    </LinearLayout>

    <!-- Take a Picture Button positioned slightly above the Choose a Picture button -->
    <Button
        android:id="@+id/btnSelectImage"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:text="Select Image"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="80dp" />

    <!-- Choose a Picture Button at the bottom -->
    <Button
        android:id="@+id/btnUploadImage"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:text="Upload"
        android:textColor="#0000FF"
        android:background="@android:color/transparent"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"/>
</RelativeLayout>
